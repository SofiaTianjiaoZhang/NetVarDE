% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_beta_covariate_variational.R
\name{estimate_beta_covariate_variational}
\alias{estimate_beta_covariate_variational}
\title{Covariate-Informed Variational EM for βᵢ}
\usage{
estimate_beta_covariate_variational(
  p_values,
  W,
  mixture,
  covariate_x,
  prior_var = 0.5,
  max_iter = 50,
  tol = 1e-04
)
}
\arguments{
\item{p_values}{Numeric vector of p-values (length n).}

\item{W}{Normalized adjacency matrix (n x n).}

\item{mixture}{List containing mixture components (f0, f1, pi0, pi1) from
\code{estimate_mixture_components}.}

\item{covariate_x}{Numeric vector of external covariate (e.g., gene sparsity,
expression level) with length n. Used to inform the prior mean of βᵢ.}

\item{prior_var}{Variance for the βᵢ hierarchical layer (default 0.5).}

\item{max_iter}{Maximum number of EM iterations (default 50).}

\item{tol}{Convergence tolerance based on change in gamma (default 1e-4).}
}
\value{
Numeric vector of estimated βᵢ (length n).
}
\description{
Estimates gene-specific network sensitivity parameters βᵢ using
variational inference with a hierarchical prior that depends on an external
covariate. The prior mean for each βᵢ is modeled as a linear function of the
covariate, allowing borrowing of information across genes.
}
\keyword{internal}
