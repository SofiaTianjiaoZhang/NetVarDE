% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ising_mean_field_beta_i_stable.R
\name{ising_mean_field_beta_i_stable}
\alias{ising_mean_field_beta_i_stable}
\title{Ising Mean Field Inference with Gene-Specific βᵢ}
\usage{
ising_mean_field_beta_i_stable(
  p_values,
  W,
  mixture,
  beta_i,
  max_iter = 100,
  tol = 1e-06,
  verbose = TRUE
)
}
\arguments{
\item{p_values}{Numeric vector of p-values (length n).}

\item{W}{Normalized adjacency matrix (n x n). Should be row-normalized
(each row sums to 1) or symmetric normalized. Use \code{normalize_network_matrix}
to preprocess.}

\item{mixture}{List containing mixture components (f0, f1, pi0, pi1) from
\code{estimate_mixture_components}.}

\item{beta_i}{Numeric vector of gene-specific βᵢ (length n), typically
estimated by \code{estimate_beta_variational} or similar.}

\item{max_iter}{Maximum number of EM iterations (default 100).}

\item{tol}{Convergence tolerance based on change in posterior probabilities
(default 1e-6).}

\item{verbose}{If TRUE, print progress messages every 10 iterations.}
}
\value{
A list with components:
\item{posterior}{Posterior probability of differential expression (vector).}
\item{parameters}{List containing alpha, beta_i, beta_global, beta_sd, pi0.}
\item{convergence}{List with iterations, converged flag, final delta.}
}
\description{
Performs mean-field variational inference for the Ising model
with gene-specific network sensitivity parameters βᵢ. This function estimates
the posterior probability of differential expression for each gene given
the p-values, network structure, and estimated βᵢ.
}
\keyword{internal}
